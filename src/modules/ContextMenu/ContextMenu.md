```jsx
<ContextMenu
  title="SweeUI Library"
  mode="gray"
  trigger="Menu"
  selected="login"
  position="right-top"
  onClick={() => { }}
>
  <div value="login">Login</div>
  <div value="create new">Create New</div>
  <div value="edit item">Edit Item</div>
</ContextMenu>
```